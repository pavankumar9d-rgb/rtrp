✅ CLOUD BASED SECURE STUDENT AUTHENTICATION SYSTEM
AWS Edition — Final Practical Version
🔷 1. SYSTEM OVERVIEW
A secure web-based authentication system for college environments that:
Uses Multi-Factor Authentication (Password + TOTP)
Protects sensitive student data using masking + step-up verification
Logs all user activity for audit and forensic purposes
Detects suspicious behavior using a rule-based risk engine
Can be deployed on AWS cloud infrastructure
🔷 2. TECHNOLOGY STACK
Layer
Technology
Backend
Flask (Python)
Frontend
HTML, CSS, Bootstrap
Database
PostgreSQL
OTP
pyotp (TOTP)
Hosting
AWS EC2
Logs (Phase 2)
AWS CloudWatch
Alerts (Optional)
AWS SNS
🔷 3. DATABASE DESIGN (FINAL)
3.1 users table
Copy code

id (PK)
username (unique)
email
phone
aadhaar
password_hash
otp_secret
failed_login_attempts (int)
account_locked (boolean)
last_login_ip
last_login_time
created_at
3.2 logs table
Copy code

id (PK)
username
event_type
ip_address
user_agent
status
timestamp
3.3 risk_events table
Copy code

id (PK)
username
risk_type
risk_score
ip_address
timestamp
🔷 4. CORE UTILITY FUNCTIONS
4.1 Get Client IP (Production Ready)
Python
Copy code
def get_client_ip(request):
    if request.headers.get('X-Forwarded-For'):
        return request.headers.get('X-Forwarded-For').split(',')[0].strip()
    return request.remote_addr
4.2 Logging Function
Python
Copy code
def log_event(username, event, status, request):
    ip = get_client_ip(request)
    agent = request.headers.get('User-Agent')

    insert into logs (username, event_type, status, ip_address, user_agent, timestamp)
🔷 5. MODULE 1: LOGIN AUTHENTICATION ENGINE
Step 1 — Username & Password
Fetch user from DB
Compare password using bcrypt
Increment failed attempts if incorrect
Lock account after 5 failures
Step 2 — OTP Verification
Generate TOTP using pyotp
Verify OTP server-side
Session Creation
Create session ONLY after both steps pass
Store:
Copy code

session['username']
session['authenticated'] = True
🔷 6. MODULE 2: DASHBOARD SECURITY CONTROL
Default State — Masked Data
Field
Display
Phone
98XXXXXX21
Email
san***@college.edu
Aadhaar
XXXX XXXX 4321
Step-up Authentication
Flow:
User clicks “View Full Details”
OTP required again
If valid:
Copy code

session['full_access'] = True
session['expires_at'] = now + 2 minutes
After 2 minutes → revert to masked view
🔷 7. MODULE 3: PASSWORD CHANGE MODULE
Inputs Required
Current password
New password
Confirm password
OTP
Backend Flow
Verify current password
Verify OTP
Validate new password:
≥ 8 chars
uppercase, lowercase, number, special char
Hash password using bcrypt
Update DB
Log event
🔷 8. MODULE 4: LOGOUT MODULE
Clear session
Invalidate cookies
Log logout event
🔷 9. MODULE 5: LOGGING MODULE (CORE)
Events to Log
LOGIN_SUCCESS
LOGIN_FAIL
OTP_SUCCESS
OTP_FAIL
VIEW_FULL
PASSWORD_CHANGE
LOGOUT
Example Log Entry
Copy code

username: sanjay
event_type: LOGIN_FAIL
ip_address: 192.168.1.5
status: fail
timestamp: 2026-04-11 10:30:00
🔷 10. MODULE 6: RISK DETECTION ENGINE (FINAL FIXED)
🔹 Approach
Rule-based scoring system using logs + IP behavior
🔹 Risk Rules
Condition
Score
Failed login
+5
Failed OTP
+10
New IP login
+10
Multiple IPs in 10 min
+15
>3 VIEW_FULL in 5 min
+10
Multiple users from same IP
+20
🔹 Risk Calculation Function
Python
Copy code
def calculate_risk(username, event, request):
    ip = get_client_ip(request)
    risk = 0

    if event == "LOGIN_FAIL":
        risk += 5

    if event == "OTP_FAIL":
        risk += 10

    if is_new_ip(username, ip):
        risk += 10

    if multiple_ips_detected(username):
        risk += 15

    if excessive_view_full(username):
        risk += 10

    if multiple_accounts_from_ip(ip):
        risk += 20

    return risk
🔹 Store Risk Events
Copy code

if risk > 0:
    insert into risk_events
🔹 Threshold Check
Copy code

total_risk = sum(last_10_min_risks)

if total_risk > 30:
    flag_user()
🔹 Action on High Risk
Temporary account lock OR
Force re-login with OTP OR
Mark as suspicious for admin
🔷 11. MODULE 7: AWS CLOUD INTEGRATION
Phase 1 (Mandatory)
Deploy Flask on EC2
Connect to PostgreSQL (RDS optional initially)
Phase 2
Send logs to CloudWatch
Phase 3 (Optional)
SNS alerts for:
High risk score
Multiple OTP failures
Basic AWS Flow
User → EC2 (Flask)
Flask → PostgreSQL
Logs → CloudWatch
Alerts → SNS
🔷 12. SECURITY CONTROLS (IMPORTANT ADDITION)
You were missing this. Now fixed.
Password hashing → bcrypt
Secure session cookies:
Copy code

HttpOnly = True
Secure = True
Account lockout after failures
OTP retry limit
Input validation (prevent injection)
Rate limiting (basic logic)
No hardcoded secrets
🔷 13. COMPLETE SYSTEM FLOW
User enters username & password
Password verified
OTP verification
Session created
Dashboard loads (masked data)
View full → OTP → temporary access
Password change → requires OTP
All actions logged (IP + timestamp)
Risk calculated per event
High risk → user flagged
(Optional) AWS alert triggered
